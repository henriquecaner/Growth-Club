# macOS
.DS_Store
.DS_Store?
Thumbs.db

# Claude Code — estado local não compartilhável
.claude/worktrees/
.claude/settings.local.json

# Cloudflare Wrangler — caches locais de dev/deploy (recriados pelo CLI)
.wrangler/
**/.wrangler/

# Sessão local de memória/logs (recriado pelo runtime)
.remember/

# Brainstorm sessions do superpowers (mockups locais, não publicar)
.superpowers/

# Contratos financeiros sensíveis — não publicar (Acordos Founder Crew, NDAs, valores comerciais)
docs/contracts/

# User research com PII (nome, WhatsApp, email, LinkedIn de respondentes) — não publicar
docs/research/private/

# Exports brutos de cadastros / submissions (Tally, Substack, WhatsApp) — PII completo
data-users/

# Stack futuro (quando tivermos código)
node_modules/
dist/
build/
.next/
.env
.env.local
.env.*.local
.envrc
*.log
__pycache__/
*.pyc
.cache/
